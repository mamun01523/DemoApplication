@{
    ViewData["Title"] = "Home Page";
}

<div class="text-center">
    <h1 class="display-4">Welcome, @Context.Session.GetString("FullName")!</h1>
    <p>You are logged in as: @Context.Session.GetString("Username")</p>
    <p>User Group: @Context.Session.GetString("UserGroupName")</p>
</div>
